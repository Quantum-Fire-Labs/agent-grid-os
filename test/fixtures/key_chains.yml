openrouter:
  owner: one (Account)
  name: openrouter
  secrets: '{"api_key": "sk-test-123"}'

openrouter_two:
  owner: two (Account)
  name: openrouter
  secrets: '{"api_key": "sk-test-456"}'

openai:
  owner: one (Account)
  name: openai
  secrets: '{"api_key": "sk-test-openai-789"}'

chatgpt:
  owner: one (Account)
  name: chatgpt
  secrets: '{"access_token": "test-access-token", "oauth_account_id": "test-account-id"}'

account_sandbox_key:
  owner: one (Account)
  name: MY_API_KEY
  secrets: '{"api_key": "secret-account-value"}'
  sandbox_accessible: true

agent_sandbox_key:
  owner: one (Agent)
  name: AGENT_SECRET
  secrets: '{"api_key": "secret-agent-value"}'
  sandbox_accessible: true

agent_override_key:
  owner: one (Agent)
  name: MY_API_KEY
  secrets: '{"api_key": "secret-agent-override"}'
  sandbox_accessible: true
