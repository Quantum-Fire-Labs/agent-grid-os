one:
  chat: one
  role: user
  content: Hello there
  sender_type: User
  sender_id: <%= ActiveRecord::FixtureSet.identify(:one) %>

two:
  chat: one
  role: assistant
  content: Hi! How can I help?
  sender_type: Agent
  sender_id: <%= ActiveRecord::FixtureSet.identify(:one) %>
